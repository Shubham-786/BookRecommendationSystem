version: '3'
services:
  app:
    build: .
    container_name: fastapi-app
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "postgresql+asyncpg://myusername:mypassword@mydbinstance.abcdefg123456.us-west-2.rds.amazonaws.com:5432/mydatabase"
